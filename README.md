# Data-Modelling-and-Business-Intelligence-MySQL-2
Work with MySQL
# Cinema Database (MySQL)

## Overview
This project demonstrates the creation and modification of a simple cinema database using MySQL. It includes table creation, data insertion, and schema updates such as adding columns and modifying structure.

---
# Employees Database Queries (MySQL)

## Overview
This project contains a collection of SQL queries performed on the `employees` database. The queries demonstrate filtering, aggregation, pattern matching, and joins across multiple related tables.
